version: '3.8'

services:
  test:
    build: 
      context: .
      dockerfile: Dockerfile.test
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      - POSTGRES_DB=timon_test
      - POSTGRES_HOST=test-db
    depends_on:
      - test-db

  test-db:
    image: postgres:15
    env_file:
      - .env
    environment:
      - POSTGRES_DB=timon_test 